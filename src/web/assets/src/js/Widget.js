import WidgetChart from '@/vue/WidgetChart.vue';

// Create our vue instance
const vm = new Vue({
    el: "#widget-content",
    components: {
        'widget-chart': WidgetChart,
    },
    data: {
    },
    methods: {
    },
    mounted() {
    },
});
